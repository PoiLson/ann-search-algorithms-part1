This the project done by the students:

Lytra Maria, 1115202200089

Mylonaki Danae, 1115202200114

Details about the project (Whatever we change or do will be writtenihere as notes)
--------------------------------------------------------------------------------------

1.) noticed a little missordering in the user's arguments input, we arrange them so 
    everything is in the same way. What we changed is only the first line of input about 
    the lsh from this:
    
    $./search –d <input file> –q <query file> –k <int> -L <int> -w <double> -ο
    <output file> -Ν <number of nearest> -R <radius> -type <flag> -lsh -range
    <true|false>

    to this:

    $./search –d <input file> –q <query file> –k <int> -L <int> -w <double> -ο
    <output file> -Ν <number of nearest> -R <radius> -type <flag> -range
    <true|false> 


